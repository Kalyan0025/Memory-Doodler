# Dream / Memory Doodler (Streamlit + Gemini + p5.js)

### Setup
```bash
git clone <your-repo>
cd dream-doodler
python -m venv .venv
source .venv/bin/activate   # (Windows: .venv\Scripts\activate)
pip install -r requirements.txt
cp .env.example .env
# open .env and paste your Gemini API key
streamlit run app.py
