# Dream / Memory Doodler (Streamlit + Gemini + p5.js)

## Quick Start
```bash
git clone <your-repo-url> dream-doodler
cd dream-doodler
python -m venv .venv
source .venv/bin/activate   # Windows: .venv\Scripts\activate
pip install -r requirements.txt
cp .env.example .env        # paste your Google GEMINI_API_KEY
streamlit run app.py
